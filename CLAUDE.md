# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

`tinbot` is currently a bootstrap/test repository with no application code, build system, or dependencies yet. It contains only:

- `readme.md` — just the project name.
- `task_list.todo` — a TODO+ formatted checklist (used with the VSCode TODO+ extension) tracking machine setup steps (installing Git, VSCode, Claude, Blender, etc.), not application tasks.

There is nothing to build, lint, or test at this point, and no established architecture to follow. Do not assume a framework, language, or project structure — check with the user or re-read this file's "Repository state" section, since it will be updated as real code is added.
